Executes a given bash command in a persistent shell session with optional timeout, ensuring proper handling and security measures.

- DO NOT use the `grep` or `rg` command, instead, use another `Grep` tool provieded to you.
- DO NOT use the `find` command to search in code. Use another `Grep` tool or
  `Task` tool provided to you.
