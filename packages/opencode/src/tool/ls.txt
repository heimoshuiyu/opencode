Lists files and directories in a given path with a tree structure. The path parameter must be absolute; omit it to use current workspace directory. Use depth parameter to control how many levels of nested directories to display (default: 1). You can optionally provide an array of glob patterns to ignore with the ignore parameter. You should generally prefer Glob and Grep tools, if you know which directories to search.